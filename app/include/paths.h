#pragma once

#include "paths.h"
#include <string>

namespace paths {
const std::string folder_path = "app";

const std::string set_parameters_path = folder_path + "/include/set_parameters.h";

const std::string parameters_path = folder_path + "/parameters/topics_and_names.yaml";
} 
